Some common **technical choices** and **intelligent defaults**, based on our specific needs and experience.

Only the engines we use are declared here.

Do NOT set "strict"ness related settings here, see the "strictest" base. This package should only relax.

REMINDER: those are defaults, packages are free to override if specific needs!
