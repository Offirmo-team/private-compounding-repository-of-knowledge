// fake file for declaring deps

// because vite react plugin expects React to be a root dep
import "react-dom"
import "react"
// because autoops can't read html yet
import "@tracer-bullet--web-app/web-core"
