This monorepo uses a tool to auto create:

- all packages root files: package.json, tsconfig.json
  - do NOT create them by hand
  - deps are auto-detected and added to the dependencies fields, no need to manually install deps
